def generate_diff(args):
    """Display DIFF between two JSON files.

    Keyword arguments:
    filename1, filename2 — files to compare (Required)
    -f --format FORMAT — output format (json by default)
    """
    print('Hooray!')


def main(args):
    generate_diff(args)


if __name__ == '__main__':
    main()
